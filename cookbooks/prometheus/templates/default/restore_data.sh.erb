#!/bin/bash

docker stop prometheus
aws s3 cp s3://sinking-database-backups/prometheus/latest.bak .
tar -xf latest.bak -C /
chown nobody:nogroup -R /opt/prometheus/storage
rm latest.bak
docker start prometheus