global:
  scrape_interval: 5s
  evaluation_interval: 1s

rule_files:
 - /rules/*.rules

alerting:
  alertmanagers:
    - static_configs:
      - targets:
        - chordata.sinkingpoint.com:9090

scrape_configs:
  - job_name: alertmanager
    static_configs:
      - targets:
        - chordata.sinkingpoint.com:9090