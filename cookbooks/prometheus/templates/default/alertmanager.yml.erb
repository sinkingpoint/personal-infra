global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'slack_channel'
receivers:
  - name: 'slack_channel'
    slack_configs:
      - api_url: "<%= @webhook_url %>"