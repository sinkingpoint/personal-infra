server {
    listen 80;
    server_name <%= @server_name %>;

    location / {
      include /etc/nginx/proxy.conf;
      proxy_pass          http://127.0.0.1:<%= @listen_port %>/;
      proxy_redirect      http://127.0.0.1:<%= @listen_port %>/ http://<%= @server_name %>;
    }
}
